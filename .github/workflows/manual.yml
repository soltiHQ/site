name: Manual

on: workflow_dispatch

concurrency:
  group: manual-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: write

jobs:
  ci:
    name: ci
    uses: soltiHQ/actions/.github/workflows/static-ci.yml@v1
